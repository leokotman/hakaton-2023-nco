import { createContext } from 'react';

// @context.js (3)
// TODO: Consider adding a small helper hook (e.g. useAppContext)
// to standardize how the context is consumed across the app.
export const AppContext = createContext(null);
